import OverviewDownloadComponent from '@/components/overview-download';

export const metadata = {
  title: 'Overview Download - Pipeline UPP',
  description: 'Export pipeline data as CSV or Excel with 9 key fields',
};

export default function OverviewDownloadPage() {
  return <OverviewDownloadComponent />;
}
