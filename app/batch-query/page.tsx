import BatchQueryComponent from '@/components/batch-query';

export const metadata = {
  title: 'Batch Query - Pipeline UPP',
  description: 'Query quotes with 10 advanced filters and view results',
};

export default function BatchQueryPage() {
  return <BatchQueryComponent />;
}
