import BatchDetailsComponent from '@/components/batch-details';

export const metadata = {
  title: 'Batch Details - Pipeline UPP',
  description: 'Edit individual quotes with editable (yellow) and read-only (gray) fields',
};

export default function BatchDetailsPage() {
  return <BatchDetailsComponent />;
}
